import java.util.Random;

public class NewCloud
{
  int x, y;
  int id;
  String col = "WHITE";
  Random rand = new Random();

  /*
  public NewCloud(int inX, int inY)
  {
    x = inX;
    y = inY;
    Ball array[] = new Ball[3];
    if (rand.nextBoolean())
      col = "GREY";
    array[0] = Ball(x - 50, y - 5, 30, col);
    array[1] = Ball(x, y, 50, col);
    array[2] = Ball(x + 50, y + 10, 35, col);
  }
  */
}
